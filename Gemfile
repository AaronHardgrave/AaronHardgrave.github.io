source 'https://rubygems.org'

# GitHub Pages gem which includes Jekyll and common plugins
gem 'github-pages', group: :jekyll_plugins

# Lanyon theme (if available as a gem)
# Note: Lanyon may need to be installed manually
# See SETUP_INSTRUCTIONS.md for details

# Additional plugins
group :jekyll_plugins do
  gem 'jekyll-remote-theme'
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
  gem 'jekyll-sitemap'
end
